# Analyst — Skills

## Required skills

Load all three skills on every run.

| Skill | Purpose |
|---|---|
| `replay-config` | Replay API key, MCP server URL, client context, React configuration |
| `replay-mcp-tools` | Full tool reference, investigation sequence, tool selection guidance |
| `fault-report-format` | Output format spec — every field the fault report must contain |
